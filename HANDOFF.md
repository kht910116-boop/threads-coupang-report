# 인수인계

이 문서는 **다른 Claude 세션이 이 프로젝트를 이어받을 때** 읽는 것이다.
사람용 설명은 `README.md`에 있다. 여기에는 코드만 봐서는 알기 어려운 것,
즉 **왜 그렇게 했는지**와 **어디까지 검증했는지**를 적는다.

- 저장소: `kht910116-boop/threads-coupang-report`
- 브랜치: `claude/website-feature-analysis-clone-lxo5b9`
- 커밋 메시지에 결정 이유를 적어뒀다. `git log`를 먼저 훑으면 맥락이 빨리 잡힌다.
- **`REFERENCE.md`** — 레퍼런스 사이트(튜브워커)를 실제로 돌려보고 정리한 화면 언어.
  확인에 크레딧 245가 들었다. **다시 보지 마라.** 거기 다 적혀 있다.

## 무엇을 만드는 중인가

레퍼런스 사이트(`tube.autoworkers.ai`)의 동작을 참고해, **주제 한 줄에서
캡컷 프로젝트까지** 끌고 가는 개인용 도구를 만든다.

알맹이는 Next.js 서버지만 **사용자가 만나는 것은 설치해서 쓰는 Windows 프로그램이다.**
Electron 창이 그 서버를 안에서 띄우고 화면을 보여준다. 검은 창도 브라우저 주소창도
없고, Node.js 설치나 `npm install`도 필요 없다. 사용자가 `유튜브 자동화` 프로젝트의
YT Studio Console처럼 "실제 프로그램"이기를 원했기 때문이다.

```
1 대본 ─ 2 구조 ─ 3 음성 ─ 4 스토리보드 ─ 5 이미지 ─ 6 영상화 ─ 7 자막·효과 ─ 8 캡컷
                        매 단계에 비서 봇이 따라다닌다
```

## 반드시 지켜야 하는 제약

### 1. API 키를 쓰지 않는다

사용자는 **모든 AI를 구독제로 쓴다.** 종량제 API 키가 없다.
그래서 경로가 셋이고, 새 기능을 붙일 때도 이 순서를 지켜야 한다.

| 경로 | 무엇 | 어디 |
|---|---|---|
| 구독 CLI | `claude -p` 같은 헤드리스 CLI | `src/lib/engine/cli.ts` |
| 구독 웹 | 로그인된 웹 UI를 백그라운드 브라우저로 조작 | `src/lib/providers/web/driver.ts` |
| 직접 넣기 | 프롬프트만 뽑고 결과 파일은 사용자가 업로드 | 각 어댑터의 `manual` |

API 어댑터도 코드에는 남아 있지만 **기본값이 아니고 검증도 안 됐다.**
새 기능을 API로만 되게 만들면 사용자에게는 없는 기능이다.

### 예외 둘 — 사용자가 정했다

**구글 두 개는 무료 한도 안에서 API로 쓴다.** 사용자가 그렇게 정했으므로 원칙 위반이
아니다. 되돌리지 말 것.

| | 무엇 | 키 |
|---|---|---|
| Google Cloud TTS | 음성 | `GOOGLE_CLOUD_TTS_API_KEY` |
| Google 생성 API (Agent Platform 자리) | 글 | `GOOGLE_GENAI_API_KEY` |

**무료 한도 안에서만**이라는 게 조건이다. 그래서 이 둘을 기본 엔진으로 올리거나
대량 작업(455줄 음성, 95장면 스토리보드)에 물리면 안 된다. 그건 구독 CLI가 한다.
생성 API는 429가 오면 "무료 한도를 넘겼다"고 따로 알려준다 — 그냥 오류로 넘기면
왜 실패했는지 알 수 없다.

### 레퍼런스가 무엇으로 돌아가는지 (사용자가 알려준 것)

| 하는 일 | 레퍼런스 | 우리가 가야 할 길 |
|---|---|---|
| 대본·스토리보드·비서 (토큰 쓰는 것 전부) | Claude **API** | `claude` CLI 구독 ✅ 검증됨 |
| 음성 | 타입캐스트 **API** | 타입캐스트 구독 웹 ❌ 레시피 URL이 편집기가 아님 |
| 이미지 | **미드저니** | **엑셀 반자동** ✅ (아래) |
| 영상 | **Google Flow** 구독 | **엑셀 반자동** ✅ (아래) |
| 보조 | Grok 구독 | `grok` CLI ✅ 검증됨 |

**사용자는 이 전부를 구독으로 쓰길 원한다.** 레퍼런스가 API를 쓰는 자리(Claude·타입캐스트)도
우리는 구독 경로로 가야 한다. 글은 이미 CLI로 되고, 남은 것은 음성·이미지·영상 세 개다.

지금 기본 미디어 레시피는 Gemini·ChatGPT 이미지와 ElevenLabs·타입캐스트 음성뿐이라
**미드저니와 Flow가 아예 없다.** 둘 다 로그인해야 DOM을 볼 수 있어서 아직 못 만들었다.
(Flow는 로그인돼 있었지만 편집기를 열려면 계정에 새 프로젝트를 만들어야 해서 멈췄다.)

### 2. 서비스 이름을 코드에 박지 않는다

CLI 플래그도, 웹 선택자도 사이트마다 다르고 수시로 바뀐다. 그래서 전부 설정으로 뺐다.

| 파일 | 무엇 |
|---|---|
| `data/agents.json` | 구독 CLI (명령·인자·프롬프트 전달 방식·결과 경로) |
| `data/web-providers.json` | 구독 웹 챗 (글자를 받아옴) |
| `data/web-media.json` | 구독 웹 파일 (이미지·음성·영상을 받아옴) |

**새 서비스를 붙이는 일이 코드 수정이 되면 설계가 틀린 것이다.** 화면에서 항목을
추가·편집할 수 있어야 한다.

### 다계정도 설정으로 한다

`data/agents.json`의 항목마다 `env`가 있다. 구독 CLI는 대개 홈 디렉터리 하나에
로그인 정보를 두므로, **그 경로만 갈아끼우면 같은 CLI를 계정별로 따로 쓴다.**

```jsonc
{ "id": "codex-2", "label": "Codex (둘째 계정)", "command": "codex",
  "env": { "CODEX_HOME": "C:\Users\나\.codex-2" } }
```

계정 하나당 항목 하나다. id와 label만 다르고 나머지는 같다. 코드는 어느 변수가
무슨 뜻인지 모른다 — 설정이 다 정한다.

빈 폴더를 CODEX_HOME으로 주면 401이 나는 것으로 실제로 갈리는 것을 확인했다.
새 계정은 그 폴더를 CODEX_HOME으로 주고 `codex` 로그인을 한 번 하면 된다.
claude는 `CLAUDE_CONFIG_DIR`이 같은 자리다.

### 3. 주석과 UI는 한국어

코드 주석은 한국어로, **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적는다.
UI 문구도 전부 한국어다.

## 설계의 축 — 이걸 모르면 코드가 안 읽힌다

### 단위가 둘이다

```
자막 라인(ScriptLine)  = 대본 최소 단위 = TTS 한 덩어리 = 자막 한 줄
장면(Scene)            = 연속된 라인 묶음 = 그림 한 장이 걸리는 단위
```

**장면은 모델이 나누지 않는다.** 라인들의 실제 음성 길이 합이 파트별 '장면 간격'
(훅 8~13초 / 파트 12~22초 등)에 들어오도록 `pipeline/grouping.ts`가 계산한다.
모델은 이미 정해진 장면에 그림 설명만 붙인다. 모델에게 개수를 맡기면 길이 기준이
안 지켜져서 이렇게 했다.

장면 묶기에는 **상한을 무조건 지킨다**는 불변식이 있다. 짧은 꼬리 장면은 앞 장면에서
라인을 한 줄씩 넘겨받아 채우고, 합쳐도 상한을 안 넘길 때만 합친다.
(예전에 무조건 합쳤다가 22초 상한인 파트에 27.6초 장면이 생기는 버그가 있었다.)

### 엔진 계약은 `complete()` 하나뿐

`src/lib/engine/types.ts`의 `Engine` 인터페이스는 메서드가 하나다.
대본 생성·스토리보드 생성·비서 대화가 전부 이걸 쓴다.
CLI·웹·API 세 경로에 같은 로직이 세 벌 생기는 걸 막으려고 이렇게 했다.

스키마 플래그가 없는 엔진에는 JSON Schema를 시스템 프롬프트에 글로 넣고,
응답에서 JSON을 건져낸다(`engine/json.ts`).

### 타임라인 계산은 한 곳에서만

`pipeline/timeline.ts`가 무음(앞/뒤/줄사이/파트사이)까지 반영한 시각을 낸다.
SRT·캡컷 드래프트·화면이 **전부 이 함수 결과를 쓴다.** 두 곳에서 계산하면
자막과 영상 시각이 어긋난다. 실제로 그 버그를 한 번 잡았다.

### 이미지 일관성

프롬프트는 `접두부(화풍) + 장면묘사 + 접미부` 구조다(`composeImagePrompt`).
접두·접미는 모든 장면에 똑같이 붙어 그림체를 고정한다.
스토리보드 프롬프트에는 **같은 인물·장소를 매번 같은 문구로 묘사하라**고 명시해뒀다.
안 그러면 같은 해설자가 장면마다 다른 사람이 된다. (이건 실제로 확인했다.)

## 이미지·영상은 반자동이다 — 자동화하려 들지 마라

사용자가 정한 것이다. **이미지와 영상 생성에는 브라우저 자동화를 붙이지 않는다.**
플로우와 그록에서 사용자가 직접 만들고, 앱은 그 앞뒤만 맡는다.

```
스토리보드 ─→ [엑셀 받기] ─→ 사용자가 플로우/그록에서 생성 ─→ [일괄 올리기] ─→ 장면에 붙음
```

미드저니·Flow 웹 레시피가 없다는 것이 오랫동안 결함으로 적혀 있었는데, 결함이 아니라
**설계다.** 레시피를 새로 쓰려고 시간을 쓰지 마라.

### 6단계는 5단계 이미지를 물려받는다 — 영상은 '대체'다

사용자가 정한 것이다. **모든 장면은 이미지를 갖는다.** 6단계는 그중 움직이면 좋을
장면만 골라 파일을 영상으로 바꾼다. 나머지는 컷 이미지 그대로 나간다.

`scene.mode`는 '이미지냐 영상이냐'가 아니라 **'영상으로 대체할 것인가'**로 읽어야
한다. 예전 코드는 배타적으로 다뤄서 이런 것들이 어긋나 있었다.

| 어디 | 예전 | 지금 |
|---|---|---|
| 6단계 목록 | `mode === "video"`인 장면만 | 전체 장면 (컷이 다 보인다) |
| 장면 카드 올리기 | `mode`로 종류 결정 → 영상 장면엔 이미지를 못 올림 | 단계가 정한다 (`uploadKind`) |
| 썸네일 | `mode==="video" && video` | 영상이 있으면 영상, 없으면 컷 |
| 준비 여부 | `mode==="video" ? !video : !image` | 이미지도 영상도 없을 때만 빈 장면 |
| 영상 표시 | 스토리보드로 돌아가 드롭다운 | 6단계에서 골라 `영상으로 표시` |

영상 파일을 일괄 업로드하면 그 장면의 `mode`가 자동으로 `video`가 된다. 표시를
따로 하게 두면 파일만 올려놓고 컷 이미지가 나가는 일이 생긴다.

캡컷 쪽은 원래부터 `mode === "video" && video !== null`로 골라서 옳았다 — 표시만
해두고 영상이 없으면 컷으로 떨어진다.

### 엑셀 형식은 레퍼런스 사이트 파일에 맞췄다

사용자가 준 실제 파일(`여덟째 가자 (1).xlsx`, 94장면)을 뜯어서 맞춘 것이다.

| | 이미지 | 영상 |
|---|---|---|
| 시트명 | `storyboard` | `storyboard` |
| 열 | 씬번호 · 한글요약 · 프롬프트 | 씬번호 · 한글요약 · 컷파일 · 프롬프트 |
| 열 너비 | 8.83 / 45.83 / 90.83 | 위 + 컷파일 28.83 |
| 대상 | 모든 장면 | `mode === "video"`인 장면만 |

**프롬프트 열은 자기완결이어야 한다.** 화풍 접두·접미를 붙여서 내보낸다
(`composeImagePrompt`). 원본 94행이 접두 한 글자까지 전부 같았다 — 셀을 그대로
복사해 쓰는 구조라서 여기서 접두를 빼면 장면마다 그림체가 흔들린다.

`src/lib/export/xlsx.ts`는 라이브러리 없이 xlsx를 쓴다. `node_modules`를 standalone
트리에 넣는 문제로 앱이 창도 못 띄우고 죽은 적이 있어서 의존성을 늘리지 않았다.
문자열 표 한 장이 전부라 100줄이면 된다.

### 파일 ↔ 장면 매칭 규칙 (틀리기 쉬웠던 자리)

`POST /api/projects/<id>/upload/batch` — `match=number`는 **파일 이름 맨 뒤 숫자 하나만**
본다. 범위 밖이거나 숫자가 없으면 붙이지 않고 `unmatched`로 돌려준다.

처음에는 '범위 안에 드는 숫자 중 마지막 것'으로 했는데 **조용히 틀렸다.** 장면이 6개일 때
`Flow_2026-09-03_99.png`는 99가 범위 밖이라 날짜의 `03`으로 떨어져 **3번 장면을 덮었다.**
규칙을 여러 개 두면 이런 일이 생긴다. 하나로 줄여야 화면에 한 문장으로 적을 수 있고,
사용자가 파일명을 그에 맞출 수 있다. **틀린 곳에 붙이느니 안 붙이는 게 낫다.**

응답은 항상 `matched` / `unmatched` / `stillEmpty` 셋을 준다. 화면이 그대로 보여준다.

## 포장할 때 딸려 나가던 것들 (고침)

`.next/standalone`에 프로젝트 루트가 통째로 추적돼 들어가고 있었다. 두 가지가 샜다.

| | 무엇 | 크기 |
|---|---|---|
| `dist/**` | 지난 포장 결과물이 다음 포장에 다시 들어감 | 433MB (돌릴수록 커짐) |
| `data/**` | **사용자의 대본·음성·API 키(`secrets.json`)** | 8.3MB |

`data/`가 더 심각하다. 남에게 설치 파일을 건네면 내 키가 같이 간다.
`outputFileTracingExcludes`로 막았고, 포장본에 `resources/server/data`가 없는 것을 확인했다.

**제외 패턴은 구체적으로 적을 것.** `dist/**`처럼 짧게 쓰면 앵커가 없어서
`node_modules/next/dist/**`까지 걸려 든다. 실제로 그렇게 해서
`app-route-turbo.runtime.prod.js`가 빠졌고, **앱은 창까지 뜨는데 모든 API가 500**을 냈다.
증상이 "서버가 안 뜬다"가 아니라 "뜨는데 아무것도 안 된다"라서 원인을 찾기 어렵다.
포장 후 `/api/projects`를 한 번 눌러 보는 것이 가장 빠른 확인이다.

## 검증 상태 — 이게 제일 중요하다

| 부분 | 상태 |
|---|---|
| 대본 생성 (구독 CLI) | ✅ 38줄 909자, 8파트, 글자수 초과 0 |
| 대본 직접 넣기 (구독 CLI) | ✅ 9,964자 → 14구간 455줄, 원문 대조 일치, 5분 25초 |
| 장면 묶기 (긴 대본) | ✅ 455줄 → 95장면, 상한 위반 0 (최대 21.9초 / 상한 22초) |
| 스토리보드 (긴 대본) | ✅ 95장면 4묶음, 8분 50초, 빈 항목 0, 묶음 넘어 인물 문구 동일 |
| 장면 묶기 | ✅ 13장면, 상한 위반 0 |
| 스토리보드 생성 | ✅ 인물이 여러 장면에 같은 문구로 묘사됨 |
| 타임라인 | ✅ 무음이 SRT·캡컷 자막에 동일 반영 |
| 웹 드라이버 (챗) | ✅ 로컬 테스트 사이트로 확인 |
| 웹 드라이버 (파일) | ✅ element 방식 PNG, download 방식 WAV |
| 음성 생성 (Google Cloud TTS) | ✅ 455줄 5.9분, 실패 0, MP3 길이 직접 파싱 |
| 장면 묶기 (실제 음성 길이) | ✅ 83장면, 상한 위반 0, 합이 음성 길이와 정확히 일치 |
| 내보내기 번들 | ✅ 455줄·95장면으로 재확인 — SRT 455블록 겹침 0, 캡컷 텍스트 트랙 455 |
| 구독 CLI 플래그 | ✅ claude·codex·grok·opencode 4종 실측 (antigravity는 미설치라 뺌) |
| 런처 스크립트 | ✅ 깨끗한 상태에서 끝까지 |
| 데스크톱 포장 (.exe) | ✅ 아래 참고 |
| 웹 레시피 — 입력창 | ✅ ChatGPT·Claude·Gemini·Grok·Perplexity 5종 확인, Grok은 틀려서 고침 |
| **웹 레시피 — 답변·결과 요소** | ❌ **미검증** (실제 대화가 있어야 보임) |
| **웹 레시피 — 음성(ElevenLabs·타입캐스트)** | ❌ **미검증** (브라우저에 로그인 세션 없음) |
| 캡컷 드래프트 스키마 | ✅ 이 PC 캡컷이 만든 진짜 드래프트와 최상위 키 36/36 일치 |
| **캡컷에서 실제로 열리는지** | ❌ **미검증** (캡컷을 띄워봐야 안다) |
| 스토리보드 엑셀 내보내기 | ✅ 실제 95장면 → 96행, openpyxl로 열림, 레퍼런스 파일과 구조 동일 |
| 일괄 업로드 (번호·순서) | ✅ 6장면에 6파일, 범위 밖·숫자 없음은 못 붙임으로 분리 |
| 영상 엑셀 (컷 + 프롬프트) | ✅ 표시한 장면만, 컷 파일명 포함 |
| 6단계가 이미지를 물려받는지 | ✅ 6장면 중 2개만 영상화 → 캡컷이 img 4 / vid 2로 고름, 경고 0 |
| **만든 엑셀을 엑셀이 여는지** | ❌ **미검증** (이 PC에 엑셀이 없다. openpyxl로만 확인) |
| 비서 — 프로젝트 고치기 | ✅ 시킨 두 줄만 고쳤고 spokenText까지 맞춤, 고른 것만 적용 |
| 비서 — 앱 소스 고치기 | ✅ 사용자가 손대던 파일은 안 건드림, 되돌리기·새 파일 삭제 확인 |
| 비서 — 모델 선택 | ✅ 잘못된 모델에 CLI가 unrecognized_model을 냄 |
| 포장본에 키가 안 들어가는지 | ✅ resources/server/data 없음 |
| API 경로 전부 | ❌ 미검증 (안 써도 됨) |

**미검증 항목을 검증된 것처럼 말하면 안 된다.** 아래에 이유를 적는다.

### 데스크톱 포장은 어디까지 확인했나

`AutoTube Studio Setup 0.1.0.exe`(89MB)가 구워지고, `dist/win-unpacked`의 실행 파일로
**창이 실제로 떴다.** 창 안에서 확인한 것:

- 창 제목·한국어 메뉴(파일·보기), 첫 화면(새 영상 폼·프리셋 드롭다운·빈 프로젝트 목록)
- 서버가 127.0.0.1의 빈 포트에서 응답, `/api/providers`가 `claude` CLI를 `ready: true`로 보고
- `_next/static`의 CSS·JS 200
- 실행 파일 옆 `app-data/`에 `agents.json`·`presets.json`·`web-providers.json` 자동 생성

**확인 안 한 것**: NSIS 설치본을 실제로 설치해 바탕화면 아이콘으로 켜보지는 않았다
(win-unpacked 실행 파일로만 확인했다). 다른 PC에서 켜보지도 않았다.

### 왜 웹 선택자가 미검증인가

작업 환경(클라우드 컨테이너)의 네트워크 정책이 GitHub·npm 외 모든 도메인을
막고 있었다. `chatgpt.com`, `gemini.google.com`, `elevenlabs.io` 전부 접속 불가.
그래서 `resultSelector: "generated-image img"` 같은 값은 **추정치**다.

대신 **드라이버 자체는 로컬에 가짜 사이트를 띄워 검증했다** — 자리표시자가 먼저 뜨고
잠시 뒤 진짜 src가 붙는 동작, 스트리밍 응답, 다운로드 버튼까지 흉내 냈다.
즉 기계는 돌고 선택자만 맞추면 된다.

**입력창은 이제 확인됐다.** 로그인된 브라우저에서 DOM을 직접 조회해 다섯 사이트의
promptSelector를 맞췄다. 두 가지를 잡았다.

- **Grok이 틀렸다.** 초안의 `textarea`는 숨은 그림자 요소이고 진짜 입력창은 tiptap
  에디터다. 예전 값으로는 입력이 그냥 사라졌다.
- **Gemini는 함정이 있었다.** `rich-textarea div[contenteditable='true']`가 둘 잡히는데
  두 번째가 Quill의 숨은 클립보드다. 드라이버가 `.first()`를 써서 우연히 동작했지만
  구글이 순서를 바꾸면 조용히 깨진다. `.ql-editor`로 못 박았다.

**아직 미검증인 것**: 답변·결과 요소(responseSelector·resultSelector)는 실제 대화가
오간 화면에서만 존재해서 새 대화 화면에서는 확인할 수 없다. 그리고 ElevenLabs·
타입캐스트는 브라우저에 로그인 세션이 없어 아예 못 봤다(타입캐스트는 레시피 URL이
편집기가 아니라 마케팅 페이지인 것만 확인했다).

**Orca 환경(이 PC)에서는 접속 제약이 깨졌다.** 확인해보니 `gemini.google.com`과
`elevenlabs.io`는 200, `chatgpt.com`과 `claude.ai`는 403이 온다 — 403은 curl을 봇으로
막은 것이지 도달 자체는 된다는 뜻이다. 로그인된 실제 브라우저로는 열릴 가능성이 높다.
다만 **이건 네트워크 도달 확인일 뿐 선택자 검증이 아니다.** 선택자는 여전히 추정치다.

### 캡컷 드래프트는 어디까지 확인했나

`draft_content.json` 스키마는 공개 문서가 없고 앱 버전마다 다르다. 그래서 예전에는
"알려진 형태를 따라 만든 추정"이었다.

**이제는 추정이 아니다.** 이 PC에 캡컷이 깔려 있고, 캡컷이 직접 만든 드래프트가
`%LOCALAPPDATA%\CapCut\User Data\Projects\com.lveditor.draft\` 아래에 있다. 그걸 우리 산출물과 대조해서 맞췄다 —
최상위 키 36개가 정확히 일치하고 materials 하위 키도 다 채웠다.

짐작이 틀렸던 자리는 이랬다.

| | 예전(짐작) | 실제 |
|---|---|---|
| `new_version` | 110.0.0 | **183.0.0** |
| `platform.app_version` | 5.9.0 | **9.3.0** |
| `platform.app_id` | 3704 | **359289** |
| `platform.os` | mac | **windows** |

윈도우 사용자의 드래프트가 자기를 맥에서 만들었다고 말하고 있었다. os와 os_version은
이제 실행 중인 PC에서 읽는다.

**여전히 미검증인 것**: 캡컷을 실제로 띄워 열리는지는 안 봤다. 스키마가 맞아도
세그먼트 내부 값이 틀리면 안 열릴 수 있다 — 진짜 드래프트의 세그먼트는 키가 50개인데
우리는 31개다.

**캡컷 버전이 다르면** `export/capcut.ts`의 `DRAFT_VERSION`과 `PLATFORM`을 고쳐야 한다.
자기 캡컷이 만든 프로젝트의 draft_content.json에서 `new_version`과 `platform`을
그대로 옮기면 된다. 드래프트가 안 열릴 때 제일 먼저 의심할 자리다.

그래서 내보내기는 **항상 두 벌**을 낸다 — 드래프트 + 범용 번들(에셋·SRT·CSV).
드래프트가 안 열려도 작업은 이어진다. **이 안전장치를 없애면 안 된다.**

효과는 캡컷 내부 effect id가 필요해서, **키프레임으로 표현 가능한 것만** 실제로 건다
(페이드·디졸브→투명도, 줌→배율, 팬→위치, 플래시→급변).
블러·글리치·오버레이는 목록으로 뽑아 사용자가 직접 걸게 안내한다.

## 다음에 할 일 (우선순위)

1. **웹 레시피 선택자 맞추기** — 연결 상태 화면의 `로그인` → `시험` 버튼으로 확인한다.
   실패 메시지에 어느 선택자가 안 맞는지 나온다. `data/web-media.json`,
   `data/web-providers.json`을 고치면 되고 코드는 안 건드려도 된다.
2. **캡컷에서 드래프트 열어보기** — 안 열리면 캡컷 버전을 확인하고
   `src/lib/export/capcut.ts`를 맞춘다.
3. **5·6단계 실제 생성 확인** — 이미지 13장이 같은 그림체로 나오는지.
4. ~~미검증 CLI 플래그~~ — 끝났다. claude·codex·grok·opencode 네 개를 실측해
   `engine/agents.ts` 기본값을 고쳤다. antigravity는 이 PC에 없어서 뺐다.
5. **레지스트리 통합** — 지금은 기획 엔진만 웹/CLI/API를 고른다.
   사용자가 원한 그림은 프로바이더 목록 하나를 두고 단계마다 골라 쓰는 것이다.
   (사용자가 보여준 드롭다운: ChatGPT / Claude / Gemini / Google AI Studio /
   Google Cloud Agent Platform / Google Cloud TTS / Google Flow / Grok /
   Vmake Labs / Genspark / Perplexity / Typecast / ElevenLabs /
   OpenCode·cli / Codex CLI·cli / Claude Code·cli / Antigravity·cli /
   OpenAI API·api / Anthropic API·api / Gemini API·api)

## 함정

- **작업물은 실행 파일 옆에 두지 않는다.** 예전에는 exe 옆 `app-data`에 뒀는데,
  다시 구울 때 electron-builder가 `dist/win-unpacked`를 통째로 지우고 새로 만들면서
  그 안의 프로젝트와 API 키까지 같이 날아갔다. 실제로 사용자가 넣어둔 키를 그렇게
  지웠다. 지금은 `%APPDATA%\AutoTube Studio\app-data`에 둔다 — 굽는 것과 무관한 자리다.
  예전 자리에 있던 것은 첫 실행 때 한 번 옮겨준다(`electron/main.js`의 resolveDataDir).
- **화면을 고쳤으면 다시 구워야 앱에 반영된다.** 이건 웹앱이 아니라 설치본이다.
  `npm run dev`로 확인하는 것은 개발 서버일 뿐이고, 사용자가 켜둔 exe는 마지막으로
  구운 시점에 멈춰 있다. "넣었습니다"라고 말하기 전에 `npm run pack`을 돌리고 앱에서
  눈으로 볼 것. 실제로 API 키 칸을 만들어 놓고 앱에는 없어서 사용자가 못 찾은 적이 있다.
- **`next dev`가 `AGENTS.md`와 `CLAUDE.md`를 자동 생성한다.** 지워도 다시 생기니
  커밋해둔 상태다. 작업 트리에 뜨면 놀라지 말 것.
- **크롬은 같은 프로파일 폴더를 동시에 두 번 못 연다.** 로그인 창이 떠 있는 동안
  그 프로바이더로 작업을 돌리면 실패한다.
- **`data/`는 gitignore다.** 프로젝트·프리셋·레시피·에셋·**API 키**가 전부 거기 있다.
  지우면 기본값부터 다시 시작한다.
- **API 키를 화면에서 넣는다**(연결 상태 → API 키). `.env`를 고치라고 하면 안 된다 —
  설치본에서는 그 파일이 설치 폴더 안에 있어서 사용자가 찾을 수 없고, 파일 편집을
  요구하는 순간 그 기능은 없는 기능이 된다. `data/secrets.json`에 저장하고
  요청마다 `loadSecretsIntoEnv()`가 process.env에 얹는다.
  **.env가 이긴다** — 이미 환경변수로 들어온 값은 덮어쓰지 않는다. 개발자가 넣어둔
  값을 화면 설정이 몰래 갈아치우면 왜 그 값이 쓰이는지 알 수 없다.
  필요한 키 목록은 어댑터의 `envKeys`에서 긁어온다. 목록을 화면에 박지 말 것.
- **오디오 길이는 헤더를 직접 읽는다**(`pipeline/audio.ts`). ffmpeg 설치를 강요하지
  않으려고 WAV 헤더와 MP3 프레임을 파싱한다. 못 읽으면 글자수 추정치로 넘어간다.
  455줄짜리 실제 MP3로 확인했다 — 추정치와 줄마다 다르게 나오므로 진짜로 읽고 있다.
- **말 속도는 프로젝트에서 배운다**(`grouping.ts`의 `makeLineDuration`).
  코드에 박힌 5.8자/초는 목소리마다 틀린다. Chirp3-HD로 재보니 6.91자/초였고,
  455줄에서 4분 22초(19%) 차이가 나면서 장면이 104개와 83개로 갈렸다.
  상수를 6.9로 바꾸는 건 답이 아니다 — 다른 목소리에서 또 틀리고, 목소리를 바꿀
  때마다 상수를 재는 건 사용자가 할 일이 아니다. 그래서 **음성이 붙은 줄 5개 이상이면
  거기서 자/초를 계산해** 나머지 추정에 쓴다. 장면 묶기·타임라인·화면이 전부 같은
  계산기를 쓴다 — 하나라도 다른 기준을 쓰면 자막과 영상 시각이 어긋난다.
- **라인을 고치면 그 줄의 음성이 지워진다.** 의도된 동작이다(글이 바뀌면 음성이
  안 맞으므로). 장면 길이도 다시 계산된다.
- **포장에서 `!node_modules` 패턴을 쓰면 안 된다.** electron-builder는 `node_modules`라는
  이름의 디렉터리를 특별 취급해서, 어떤 패턴을 쓰든 `files`로 넣은 트리 안의 것까지 빼버린다.
  그래서 서버 트리는 `files`가 아니라 `extraResources`로 통째로 복사한다. 이걸 되돌리면
  `.next/standalone/node_modules`가 사라지고 **창이 아무 말 없이 안 뜬다.** 두 번 겪었다.
- **`package.json`에 `dependencies`를 되살리면 안 된다.** electron-builder가 그걸 보고 루트
  node_modules를 통째로 패키지에 복사한다. 포장된 앱이 실행 시점에 필요로 하는 것은
  electron뿐이고, 서버가 쓰는 모듈은 `next build`가 이미 standalone에 추려 넣었다.
- **`outputFileTracingExcludes`를 지우면 안 된다.** Next가 `electron` 패키지(브라우저
  바이너리 포함)까지 서버 번들로 끌고 들어와 32MB짜리가 305MB가 된다.
- **`npm run prepare:standalone`을 빼먹으면** 앱은 뜨는데 스타일과 스크립트가 전부 404다.
  Next는 standalone에 `.next/static`을 넣지 않는다(CDN에 올릴 것을 전제한다).
- **스토리보드는 24개씩 나눠 부른다**(`describeInBatches`). 한 번에 95개를 요청했더니
  모델이 97개를 돌려줬고, 개수가 어긋나면 장면과 프롬프트가 밀려 전부 못 쓴다.
  나눠 부르면 뒤 묶음이 앞을 못 보므로 앞 프롬프트 8개를 같이 넘겨 인물 문구를
  이어받게 한다. 이 carry-over를 빼면 같은 해설자가 묶음마다 다른 사람이 된다.
- **대본 직접 넣기는 원문 대조가 안 맞으면 저장하지 않는다**(`pipeline/generate.ts`의
  `splitScript`). 모델은 "고치지 마라"라고 시켜도 종종 다듬는다. 공백을 지운 뒤 한 글자라도
  다르면 실패로 돌린다. 이 검사를 빼면 사용자가 공들여 쓴 대본이 조용히 바뀐다.
- **CLI를 새로 붙일 때 확인할 것은 셋이다** — 프롬프트를 stdin으로 받는지(긴 대본이
  인자 길이 제한에 걸린다), 스키마 플래그가 인라인 문자열인지 파일 경로인지,
  답이 stdout에 단독으로 나오는지. 셋 다 도움말만 봐서는 알 수 없어 직접 갈라 봐야 한다.
  codex는 `--output-schema`가 파일 경로라 스키마를 못 쓰고, grok은 `-p`가 인자라
  긴 대본에서 막힌다.
- **약관** — 로그인된 웹 UI 자동 조작은 대부분 서비스 약관의 금지 항목이다.
  사용자는 이 점을 알고 진행하기로 했다. 다시 설득하려 들 필요는 없지만,
  기능을 넓힐 때 이 사실이 바뀌지는 않는다.

## 개발 명령

```bash
npm install
npm run dev          # http://localhost:3000 — 고치는 중에는 이게 제일 빠르다
npm run typecheck
npm run app          # 빌드해서 실제 창으로 띄운다
npm run pack         # dist/win-unpacked/AutoTube Studio.exe (설치 없이 실행)
npm run dist         # dist/AutoTube Studio Setup 0.1.0.exe (배포용 설치본)
```

`시작하기-Windows.bat` / `시작하기-Mac.command`는 설치본이 생기기 전의 실행 방법이다.
Node.js가 깔린 개발 환경에서만 의미가 있다.
