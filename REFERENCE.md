# 레퍼런스 — 튜브워커에서 가져온 화면 언어

`tube.autoworkers.ai`(튜브워커)를 사용자 계정으로 **6단계 전부 실제로 돌려보고** 정리한 것이다.
이 문서가 있는 이유는 하나다 — **다시 보지 않기 위해서**. 확인에 크레딧 245가 들었고
(설계도 14 · 발음변환 17 · TTS 126 · 스토리보드 88), 같은 걸 또 볼 이유는 없다.

우리와 개념은 같다. 단계 마법사, 파트별 장면 간격, 파트 필터, 캡컷 내보내기까지.
**다른 것은 개념이 아니라 화면 언어였다.** 아래가 그 언어다.

## 그쪽 6단계 (우리 8단계와의 대응)

| 튜브워커 | 우리 |
|---|---|
| 1 대본 | 1 대본 + 2 구조 |
| 2 음성 | 3 음성 |
| 3 스토리보드 | 4 스토리보드 |
| 4 이미지 | 5 이미지 |
| 5 영상 | 6 영상화 |
| 6 완성 | 7 자막·효과 + 8 캡컷 |

URL은 `/wizard/<id>/step/<slug>/`이고 slug는 `script` `voice` `storyboard` `images` `video` `completion`.

**주의** — 이 사이트는 URL로 지난 단계에 들어가면 프로젝트의 '현재 단계'가 실제로
거기로 되돌아간다. 데이터는 안 지워지지만 목록의 단계 표시가 바뀐다. 원상복구는
`다음` 버튼으로 걸어가는 수밖에 없다. 남의 계정에서 함부로 눌러보지 말 것.

## 가져온 것 12가지

### 골격

1. **수평 스텝 레일** — 끝난 단계는 ✓, 지금은 채운 알약, 남은 것은 흐린 숫자, 사이는 연결선.
   알약만 나열하면 탭처럼 보여서 순서가 있는 절차라는 게 안 읽힌다.
   (그쪽은 레일이 클릭 불가고 뒤로 못 간다. 우리는 로컬 도구라 되돌아가 고치는 일이
   훨씬 잦으므로 이동을 막지 않기로 했다.)
2. **모든 단계가 같은 뼈대** — `제목 + 한 줄 설명` → 카드 몇 개 → 하단 `이전`(좌) / `다음`(우, 조건 안 맞으면 비활성).
3. **빈 상태에 문장 두 줄** — 제목 한 줄 + 설명 한 줄이 고정 형식이다.
   예: "설계도를 만들면 여기에 구간이 표시됩니다. / 훅·인트로부터 마무리까지, 대본이 어떻게 나뉘는지 보여줍니다."

### 입력

4. **갈림길은 드롭다운이 아니라 카드 두 장** — `AI로 대본 생성` / `직접 입력`, 각각 설명 한 줄.
   고르고 나면 작은 라벨 + `방식 바꾸기` 링크로 접힌다.
5. **세그먼트 컨트롤 + 우측 라이브 힌트.** 이게 제일 크다. 숫자를 받지 않고 이름 붙인
   선택지를 주며, 오른쪽 힌트가 "그래서 어떻게 되는지"를 말로 알려준다. 고를 때마다 바뀐다.

   | 항목 | 선택지 | 힌트 |
   |---|---|---|
   | 자막 최대 글자수 | 적게 / 중간 / 많음 | "한 줄 최대 20자" ↔ "25자" |
   | 무음 압축 | 끄기 / 약하게 / 보통 / 강하게 / 최대 | "무음을 그대로 둡니다" / "권장 — 어색하지 않은 선에서 줄입니다" / "거의 모든 무음을 최소로 줄입니다" |

6. **작성 가이드를 입력칸 바로 위에** — 대본 규칙 6개(문장 끝 온점 뒤 띄어쓰기, 따옴표 피하기,
   문단은 줄바꿈, 특수문자·이모지 금지, URL·이메일 금지, 단어 뒤 괄호 금지)를 붙여넣는 자리 옆에 둔다.
   다 넣고 나서 읽으라고 하면 아무도 안 읽는다. 전부 TTS가 이상하게 읽는 것들이다.

### 작업 중

7. **진행 표시를 말로 쓴다** — "구간 6개의 설계도를 쓰는 중... 45%", "대본 구간을 자막에 맞추는 중... 15%",
   "발음 변환중... 10%", "음성 생성중... 50%". 스피너만 돌리지 않고 무엇을 몇 개 하는 중인지 쓴다.
   진행 중에는 입력이 잠기고 라벨이 "생성 중에는 입력할 수 없습니다"로 바뀐다.
8. **`생성 상태` 카드 하나가 3역** — 시작 전 안내 → 진행 → 끝난 뒤 다음 할 일.
   자리가 안 움직인다. 카드가 나타났다 사라지면 아래 내용이 튀어서 읽던 자리를 잃는다.
9. **비용·비가역 확인 모달** — `-14 크레딧`을 크게, `입력한 대본 9,964자 기준`, `현재 잔액: 1286`,
   "오류로 인한 실패시 소모된 크레딧은 환급됩니다". 비가역 전환도 같은 형식으로 묻는다:
   "이 대본으로 진행하시겠습니까? / 이후 단계부터는 이 대본 설계도를 이용합니다. / 대본 단계로는 돌아올 수 없습니다."

### 결과 보기

10. **설계도 = 본문 + 우측 주석 거터.** 왼쪽에 대본, 구간 경계에 세로선, 오른쪽 거터에
    구간명(`훅+인트로`) + 그 구간의 의도 한 문장 + 글자수(`623자`). 구간 목록을 따로 두지 않는다.
    따로 두면 본문과 눈이 왔다 갔다 해야 한다.
11. **발음 검수 — 바뀐 줄만.** AI가 숫자·영문을 한글 발음으로 바꾼 뒤, **바뀐 줄만** 번호와 함께
    `1,030억 원을 → 천삼십억 원을`로 보여주고 9페이지로 나눈다. 클릭하면 **범위 조정**
    (왼쪽/오른쪽 확장·축소 버튼)까지 된다. AI가 한 일을 사람이 훑는 게이트를 명시적으로 만든 것.
    기본 동작은 `이대로 생성`이라 검수는 강제가 아니지만 눈에 띈다.
12. **파트 필터 알약 + 개수**(`훅+인트로 8 / 파트1 25 / … / 클로징 11`),
    **구간 페이저**(`« 이전  1/6 구간  다음 »`),
    **씬 배지**(`씬 6 [재점화] 자막 23~26 대체 가능`. 재점화는 색으로 강조),
    **결과 배지**(`대기` / `완료`).

## 우리가 안 가져온 것

- **크레딧** — 우리는 구독제라 셀 것이 없다. 확인 모달은 남기되 "무엇을 잃는지"로 바꿨다.
  (예: "이미 만들어 둔 음성 12개가 지워집니다.")
- **뒤로 못 가게 막기** — 로컬 도구라 되돌아가 고치는 일이 잦다.
- **발음 변환** — 별도 단계로 두지 않았다. 필요해지면 TTS 앞에 붙일 자리다.
- **보이스 카드 캐러셀** — 우리는 서비스가 설정(`data/*.json`)으로 열려 있어서
  목록을 코드가 알 수 없다. 드롭다운을 유지한다.

## 확인하지 못한 것

- **5단계 영상** 화면을 한 번도 열지 않았다.
- **음성 완료 화면**(오디오 플레이어·자막 결과)을 못 봤다. 50%까지만 보고 다음 단계로 넘어갔다.
  자막은 마지막 단계에서 나오므로 우리 설계와는 무관하다.
- **이미지가 생성되는 중**인 화면. 대기/완료 상태는 봤다.

## 2차 조사 — 완성된 프로젝트 10개를 훑고 얻은 것

크레딧을 쓰지 않는 조건으로 채널의 프로젝트 10개(경제 첫 영상 ~ 여덟째 가자)를
각자의 **현재 단계 그대로** 열어봤다. `/wizard/<id>/`는 저장된 현재 단계로 보내주므로
단계가 바뀌지 않는다. 단계 URL을 직접 치면 현재 단계가 그쪽으로 옮겨간다 — 하지 말 것.

### 사용자는 완성 단계 설정을 한 번도 바꾸지 않았다

완성된 7편의 값이 **전부 똑같았다.** 기본값 그대로다.

```
자막 위치 하단 · 켄번즈 없음 · 씬 전환 컷(없음)
자막색 #FFFFFF · 배경 #FFFFFF 60% · 테두리 #000000 굵기 8 · 폰트 크기 50
```

7편을 만드는 동안 아무도 손대지 않은 설정이라는 뜻이다. 우리 7단계가 자막 관련
입력을 여덟 개 노출한 것은 과했을 수 있다. (다만 우리는 미리보기를 붙여서 바꿀
근거는 생겼다.)

### 5단계 이미지 — 스톡 영상 혼합

우리에게 아예 없는 기능이다. 씬 일부를 AI 이미지 대신 **스톡 영상**으로 채운다.

```
안 씀 — 전부 이미지 / 조금 (20%) / 보통 (35%) / 많이 (50%)
머리글에 결과 표시: "스톡 0씬 · 이미지 94씬"
```

또 **`실패한 N씬 다시 생성`** 버튼이 `전체 재생성`과 따로 있다. 95장을 만들다 몇 장
실패했을 때 전부 다시 만들게 하면 안 된다는 뜻이다. 실패한 씬에는 "생성에
실패했습니다."가 붙고 씬마다 `편집`과 `이미지 올리기`가 있다.

### 6단계 영상 — 씬을 골라서 만든다

우리는 씬마다 모드를 'AI 영상'으로 바꿔두고 한꺼번에 돌리는데, 그쪽은 **고르고**
만든다. `파트 선택`으로 파트 단위 일괄 선택도 된다.

안내문이 특히 좋다. 사용자가 부딪힐 질문을 미리 다 답한다.

> 씬을 골라야 만듭니다. 몇 개든 한 번에 고를 수 있고, **시작하면 멈출 수 없습니다.**
> 만들지 못한 씬의 크레딧은 자동으로 돌려드립니다. **영상은 선택 산출물이라 하나도
> 만들지 않고 다음 단계로 넘어가도 됩니다.** 직접 편집한 MP4(5~12초, 100MB 이하)를
> 씬마다 '올리기'로 넣을 수도 있습니다 — **올린 영상의 소리는 내레이션과 겹치지 않게
> 제거됩니다.** 영상이 이미 있는 씬은 그 영상을 삭제한 뒤에 올릴 수 있습니다.

여섯 문장이 각각 하나씩 답한다 — 어떻게 고르나 / 취소되나 / 실패하면 돈은 / 꼭
해야 하나 / 내 파일 넣어도 되나 / 소리가 겹치지 않나 / 이미 있으면. 우리 업로드
버튼에는 이런 안내가 하나도 없다.

### 우리에게 없는 것 (정리)

1. **스톡 영상 혼합** — 외부 스톡 서비스가 필요해 지금은 못 붙인다.
2. **영상 단계 씬 선택 + 파트 단위 선택** — 순수 UI다. 붙일 수 있다.
3. **업로드 제약을 미리 말해주는 안내문** — 길이·용량·소리 제거·덮어쓰기 규칙.
4. **실패한 것만 다시** — 우리는 `만들기`가 이미 없는 것만 만들지만 라벨이 그렇게
   읽히지 않는다. 실패 표시도 없다.

### 우리에게 있고 그쪽에 없는 것

- **직접 넣기가 모든 단계에** 있다. 그쪽은 대본 단계에만 있다.
- **스타일 프리셋** — 그쪽은 채널 단위 그림체 하나뿐이라 한 채널에 롱폼과 쇼츠를
  섞기 어렵다.
- **8단계로 쪼갬** — 구조(2)와 자막·효과(7)가 따로 있다. 그쪽은 6단계다.